#include <iostream>
#include <math.h>
#include <algorithm>
#include <vector>

using namespace std;

int main()
{
	int a, b, count = 0;
	cin >> a >> b;
	for (int i = 1; i*i < b + 1; i++)
	{
		if (i*i >= a && i * i <= b)
			count++;

	}

	cout << b - a + 1 - count;

	return 0;
}
